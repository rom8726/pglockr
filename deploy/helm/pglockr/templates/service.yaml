apiVersion: v1
kind: Service
metadata:
  name: {{ include "pglockr.fullname" . }}
  labels:
    {{- include "pglockr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "pglockr.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
